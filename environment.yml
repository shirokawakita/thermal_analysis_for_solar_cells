name: geo-panel-thermal
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.24
  - pandas>=2.0
  - pyyaml>=6.0
  - pip
  - pip:
      - plotly>=5.18
      - kaleido>=0.2.1
